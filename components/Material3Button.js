import Button from "@mui/material/Button";
import styled from "@mui/material/styles/styled";

const Material3Button = styled(Button)({
  height: "40px",
  borderRadius: "40px",
  textTransform: "none",
  margin: "5px",
});

export default Material3Button;
